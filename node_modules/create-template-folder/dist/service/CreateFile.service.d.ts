import { JoinRepo, MakeFileNameRepo, ReadFileRepo, WriteFileRepo } from "../utils/IO";
interface Execute {
    file: string;
    outDir: string;
    prevPath: string;
    vars: Record<string, string>;
    number: number;
}
interface ICreateFile {
    readFileRepo?: ReadFileRepo;
    makeFileNameRepo?: MakeFileNameRepo;
    writeFileRepo?: WriteFileRepo;
    joinRepo?: JoinRepo;
}
export declare class CreateFile {
    readFileRepo: ReadFileRepo;
    makeFileNameRepo: MakeFileNameRepo;
    joinRepo: JoinRepo;
    writeFileRepo: WriteFileRepo;
    constructor(args?: ICreateFile);
    execute({ prevPath, vars, outDir, file, number, }: Execute): Promise<string>;
}
export {};
