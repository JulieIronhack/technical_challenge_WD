import { CreateTemplateConfig } from "../config";
import { CreateFileOptions, ICreateRepo, IFsOperations } from "../types";
import { IPathRepo } from "../types/PathRepo.interface";
export declare class CreateService implements ICreateRepo {
    private config;
    private pathRepo;
    private fsOps;
    constructor(config?: CreateTemplateConfig, pathRepo?: IPathRepo, fsOps?: IFsOperations);
    folder(outDir: string, relPath: string, vars?: Record<string, string>, number?: number): Promise<void>;
    file({ prevPath, vars, outDir, file, number }: CreateFileOptions): Promise<string>;
}
