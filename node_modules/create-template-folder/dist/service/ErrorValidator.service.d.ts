import { Executor, ICreateTemplate } from "../types";
import { FSExistsRepo } from "../utils/IO";
export declare class ObjectErrorValidator implements Executor {
    private fsExistRepo;
    constructor(fsExistRepo?: FSExistsRepo);
    execute(value: ICreateTemplate): BoolStr;
    noInDir(inDir: string): boolean;
    outDirExists(outDir: string): boolean;
}
export declare class StringErrorValidator implements Executor {
    private fsExistRepo;
    constructor(fsExistRepo?: FSExistsRepo);
    execute: (inDir: string, outDir?: string | undefined, _vars?: Record<string, string> | undefined, number?: number | undefined) => BoolStr;
}
declare type BoolStr = false | string;
export {};
