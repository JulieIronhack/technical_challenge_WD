import { CreateTemplateConfig } from "../config";
import { Executor } from "../types";
import { JoinRepo, MkDirPRepo } from "../utils/IO";
interface ICreateFolder {
    joinRepo?: JoinRepo;
    mkdirRepo?: MkDirPRepo;
    config?: CreateTemplateConfig;
}
export declare class CreateFolder implements Executor, ICreateFolder {
    joinRepo: JoinRepo;
    mkdirRepo: MkDirPRepo;
    config: CreateTemplateConfig;
    constructor(args?: ICreateFolder);
    execute(outDir: string, relPath: string, vars?: Record<string, string>, number?: number): Promise<void>;
}
export {};
