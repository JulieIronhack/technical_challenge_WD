import { IFsOperations, ISyncFsOperations } from "../types";
export declare class GetAllServiceSync {
    private fsOps;
    _folders: Set<string>;
    _files: Set<string>;
    constructor(fsOps?: ISyncFsOperations);
    private getAllFolders;
    private setFolders;
    get folderArr(): string[];
    folders(source: string, base: string): string[];
    private getAllFiles;
    private setFiles;
    get fileArr(): string[];
    files(source: string, baseName: string): string[][];
}
export declare class GetAllService {
    private fsOps;
    _folders: Set<string>;
    _files: Set<string>;
    constructor(fsOps?: IFsOperations);
    private getAllFolders;
    private setFolders;
    get folderArr(): string[];
    folders(source: string, base: string): Promise<string[]>;
    private getAllFiles;
    private setFiles;
    get fileArr(): string[];
    files(source: string, baseName: string): Promise<string[][]>;
}
