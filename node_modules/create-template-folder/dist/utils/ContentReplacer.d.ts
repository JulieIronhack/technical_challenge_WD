export declare class Content {
    static get sep(): "/" | "\\\\";
    static makeFileName(file: string): string;
    static makeFolderStruct(folders: string[], base: string): string[];
    static makeFileStruct(files: string[], base: string): string[][];
    static replace(str: string, vars?: Record<string, string>, number?: number): string;
}
