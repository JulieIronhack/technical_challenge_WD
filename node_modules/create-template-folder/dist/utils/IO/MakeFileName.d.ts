/// <reference types="node" />
import { sep } from "path";
import { Executor } from "../../types";
export declare class MakeFileNameRepo implements Executor {
    private seperator;
    constructor(seperator?: typeof sep);
    get sep(): "/" | "\\\\";
    execute(file: string): string;
}
