import { CreateTemplateConfig } from "../../config";
import { Executor } from "../../types";
export declare class WriteFileRepo implements Executor {
    private config;
    constructor(config?: CreateTemplateConfig);
    execute(realPath: string, content: string): Promise<void>;
}
