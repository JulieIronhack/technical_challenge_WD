/// <reference types="node" />
import { mkdirSync } from "fs";
import { CreateTemplateConfig } from "../../config";
import { Executor } from "../../types";
export declare class MkDirPRepo implements Executor {
    private mkdirpFunc;
    private config;
    constructor(mkdirpFunc?: typeof mkdirSync, config?: CreateTemplateConfig);
    execute(thePath: string): string | undefined;
}
