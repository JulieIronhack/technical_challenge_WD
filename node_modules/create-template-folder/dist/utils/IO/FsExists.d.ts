/// <reference types="node" />
import { Executor } from "../../types";
import { existsSync } from "fs";
export declare class FSExistsRepo implements Executor {
    private existFunc;
    constructor(existFunc?: typeof existsSync);
    execute(path: string): boolean;
}
