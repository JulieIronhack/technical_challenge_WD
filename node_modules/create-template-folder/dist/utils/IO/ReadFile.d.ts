/// <reference types="node" />
import { readFileSync } from "fs";
import { Executor } from "../../types";
export declare class ReadFileRepo implements Executor {
    private readFile;
    constructor(readFile?: typeof readFileSync);
    execute(prevPath: string): string;
}
