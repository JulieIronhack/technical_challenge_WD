/// <reference types="node" />
import { sep } from "path";
import { Executor } from "../../types";
export declare class SplitSepRepo implements Executor {
    private seperator;
    constructor(seperator?: typeof sep);
    get sep(): "/" | "\\";
    execute(str: string): string[];
}
