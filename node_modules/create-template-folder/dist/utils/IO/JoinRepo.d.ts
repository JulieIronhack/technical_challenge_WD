/// <reference types="node" />
import { join } from "path";
import { Executor } from "../../types";
export declare class JoinRepo implements Executor {
    private joiner;
    constructor(joiner?: typeof join);
    execute(...files: string[]): string;
}
