import { StyledHeader, HeaderLink } from "./styled/Header.styled";
import StyledLink from "./styled/StyledLink";

const Header = () => (
    <StyledHeader>
        <HeaderLink to="/">Home</HeaderLink>
    </StyledHeader>
);

export default Header;